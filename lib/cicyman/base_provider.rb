module Cicyman
  class BaseProvider

    #method to get the file given the file id as a string
    def self.get_file(id)
    end

    #method to set the file given the file id as a string and the binary content of the file
    def self.set_file(id, binary)
    end

    #method to save the file
    def self.save_file
    end

    #method to destroy the file given the file id as a string
    def self.destroy_file(id)
    end
  end
end
